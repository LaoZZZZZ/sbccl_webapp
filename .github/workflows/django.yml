name: Django CI

on: push

jobs:
  container-job:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.9]
    services:
      postgres:
        image: postgres:latest
        env:
          POSTGRES_DB: postgres
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: huiyang@2023
          POSTGRES_PORT: 5433
          POSTGRES_HOST: localhost
        ports:
          - 5433:5433
        # need to wait for the database to be ready.
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v3
        with:
          python-version: ${{ matrix.python-version }}
      # - name: Install PostgreSQL client
      #   run: |
      #     apt-get update
      #     apt-get update
      #     apt-get install --yes postgresql-client
      # - name: Test database connection
      #   run: psql -h localhost -p 5433 -d postgres -U postgres -c 'SELECT 1;'
      #   env:
      #     PGPASSWORD: huiyang@2023

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      # - name: Install database
      #   run: |
      #     psql -h localhost -d postgres -U postgres -c  'GRANT ALL PRIVILEGES ON DATABASE postgres TO postgres;' -U postgres
      #   env:
      #     PGPASSWORD: huiyang@2023
      - name: Run Tests
        run: |
          python manage.py test members.serializers_test
